name: Build Android APK (TWA / Bubblewrap)

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Set up Java JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Bubblewrap CLI
        run: npm install -g @bubblewrap/cli

      - name: Build APK using Bubblewrap
        run: |
          # 1. إعداد ملف إعدادات Bubblewrap لتفادي الأسئلة التفاعلية
          mkdir -p ~/.bubblewrap
          cat <<EOF > ~/.bubblewrap/config.json
          {
            "jdkPath": "${JAVA_HOME}",
            "androidSdkPath": "${ANDROID_HOME}"
          }
          EOF

          # 2. الانتقال إلى مجلد twa والتحقق من وجود twa-manifest.json
          if [ -d "twa" ]; then
            cd twa
            if [ -f "twa-manifest.template.json" ] && [ ! -f "twa-manifest.json" ]; then
              cp twa-manifest.template.json twa-manifest.json
            fi
          fi

          # 3. تشغيل عملية البناء
          bubblewrap build

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-release-apk
          path: |
            **/*.apk
            **/*.aab
