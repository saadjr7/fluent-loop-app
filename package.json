{
  "name": "fluent-loop-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:android": "npx cap sync android && cd android && ./gradlew assembleDebug --no-daemon"
  },
  "dependencies": {
    "@capacitor/core": "^6.2.1",
    "@jofr/capacitor-media-session": "^4.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.1",
    "@capacitor/android": "^6.2.1"
  }
}
